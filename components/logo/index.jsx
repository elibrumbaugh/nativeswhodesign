import React from "react";

const Logo = (_props) => {
  return (
    <img
      width={250}
      src="/opengraph.png"
      aria-label="Native Talent in Tech Logo"
    />
  );
};

export default Logo;
